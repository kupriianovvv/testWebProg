в папке backend В терминале написать:

`npm i`

 `node server`
 
откройте index.html. 

каждая кнопка шлет соответствующий запрос. 

Я не сильно заморачивался с тем, чтобы проверять корректность пользовательского ввода, думаю, это несильно важно
